{
    "commands": [
        {
            "name": "help",
            "description": "display help information"
        },
        {
            "name": "clone",
            "description": "clone a repository from github",
            "option": {
                "name": "uri",
                "description": "the uri of the repository to clone",
                "required": false
            }
        },
        {
            "name": "env",
            "description": "manage env var files",
            "option": {
                "name": "action",
                "description": "the action to perform with env var files",
                "required": true,
                "allowedValues": ["create"]
            }
        }
    ]
}
